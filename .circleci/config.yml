version: 2

jobs:
  lint:
    working_directory: ~/reviewdog
    docker:
      - image: circleci/golang:1.16
    steps:
      - checkout
      - run:
          name: System information
          command: 'echo "Golang $(go version)"'
      - run: 'curl -sfL https://raw.githubusercontent.com/reviewdog/reviewdog/master/install.sh| sh -s -- -b $(go env GOPATH)/bin'
      - run: go get -u golang.org/x/lint/golint
      - run:
          command: |-
            set +o pipefail
            gofmt -s -d . | reviewdog -name="gofmt" -f=diff -f.diff.strip=0 -reporter=github-pr-review -fail-on-error -tee
      - run: go vet ./... 2>&1 | reviewdog -f=govet -reporter=github-pr-check
workflows:
  version: 2
  lint:
    jobs:
      - lint
